.MODEL SMALL
.STACK 100H
.DATA
ARR DB 3 DUP('A')
.CODE
MAIN PROC
    MOV AX, @DATA
    MOV DS, AX
    MOV ES, AX

    MOV SI, OFFSET ARR
    MOV DI, OFFSET ARR
    MOV CX, 3

    REP MOVSB

    MOV AH, 4CH
    INT 21H
MAIN ENDP
END MAIN